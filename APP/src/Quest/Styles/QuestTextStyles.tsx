import {StyleSheet} from 'react-native';

export const textStyles = StyleSheet.create({
  today: {fontSize: 16},
  small: {fontSize: 16, color: '#fff'},
  normal: {fontSize: 24, fontWeight: 'bold', color: '#fff'},
});

export default textStyles;
