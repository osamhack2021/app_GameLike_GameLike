#초기화
react-native init <폴더명> --template react-native-template-typescript

#package.json scripts 부분 수정
"scripts": {
    "bundle": "react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res"
  },

#yarn install

#mkdir android/app/src/main/assets

#build
npm run bundle && cd android/ && ./gradlew assembleDebug


#Module '"react-native"' has no exported member <string>
npm i -D @types/react-native

#git에서 바로 내려받았을 경우
npm install     //package.json 참고해서 바로 다 설치